import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AI Furniture Designer",
  description: "A creation of Murphy Al-Saham",
  icons: {
    icon: "/images/logo-largesmall.webp", // Ensure this path is correct relative to the public folder
    shortcut: "/images/logo-largesmall.webp",
    apple: "/images/logo-largesmall.webp", // Path to the apple touch icon
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
